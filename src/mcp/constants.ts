/**
 * MCP-related constants
 */

/**
 * Default health check interval in milliseconds
 * Can be overridden via SwarmConfig.healthCheckInterval
 */
export const DEFAULT_HEALTH_CHECK_INTERVAL_MS = 60000; // 1 minute

/**
 * Default connection timeout for MCP servers in milliseconds
 */
export const DEFAULT_MCP_CONNECTION_TIMEOUT_MS = 30000; // 30 seconds

/**
 * Default request timeout for registry searches in milliseconds
 */
export const DEFAULT_REGISTRY_REQUEST_TIMEOUT_MS = 5000; // 5 seconds

